[project]
name = "wsl-library"
version = "0.1.0"
description = "Core library for policy analysis"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wsl_library"]