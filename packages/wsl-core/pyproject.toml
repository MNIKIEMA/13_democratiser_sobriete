[project]
name = "wsl-core"
version = "0.1.0"
description = "Core library for policy analysis"
requires-python = ">=3.10"
dependencies = ["pydantic>=2.10.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wsl_core"]